{% extends "@pp/order/main.twig" %}
{% set menu_name="订单管理"%}
{% block content %}
订单ID:<h3>{{ order.id }}</h3>
修改订单状态为:
<form action="/pp/order/changestatus" method="POST">
	<input type="hidden" name="order_id" value="{{ order.id }}">
	<select name="status">
		{% for k,v in status %}
		<option {% if order.status == k %} selected {% endif %} value="{{ k }}">{{ v }}</option>
		{% endfor %}
	</select>
	<input type="submit" value="提交">
</form>
国家:{{ shipping.country }}&nbsp;&nbsp;城市:{{ shipping.city }}&nbsp;&nbsp;邮寄地址:{{ shipping.address }} &nbsp;&nbsp;收件人:{{ shipping.firstname}}{{ shipping.lastname }}&nbsp;&nbsp;电话:{{ shipping.phone }}<br />
国家:{{ billing.country }}&nbsp;&nbsp;城市:{{ billing.city }}&nbsp;&nbsp;付款地址:{{ billing.address }} &nbsp;&nbsp;收件人:{{ billing.firstname}}{{ billing.lastname }}&nbsp;&nbsp;电话:{{ billing.phone }}<br />
<table class="items table">
<thead>
	<tr>
		<th id="app-grid_c1">商品ID</th>
		<th id="app-grid_c4">商品名称</th>
		<th id="app-grid_c4">商品图片</th>
		<th id="app-grid_c4">商品属性</th>
		<th id="app-grid_c4">商品数量</th>
		<th id="app-grid_c4">商品单价</th>
		<th id="app-grid_c4">商品单价</th>
	</tr>
</thead>
<tbody>
{% for d in detail %}
<tr class="odd">
	<td>{{ d.product_id }}</td>
	<td>{{ d.product.name }}</td>
	<td><img width="200px" height="114px" src="{{ d.product.logo }}" /></td>
	<td>{{ this.getProfile(d.product_meta) }}</td>
	<td>{{ d.product_quantity }}</td>
	<td>{{ d.product_price }}</td>
</tr>
{% endfor %}
</tbody>
</table>
{% endblock %}